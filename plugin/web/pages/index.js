import { Button } from "zent";

export default () => (
    <div>
        Welcome to next.js! In plugin web
        <Button bordered={false} type="primary">
            无边框
        </Button>
    </div>
);
